#include <stdio.h>

int main(){
 int a;
 int b;
 int c = b;

 // print -
 printf("enter number a:");
 // scanf - read input salve in variable
 scanf("%d",&a);
 printf("enter number b:");
 scanf("%d",&a);

 if (a = a){printf("reflexive")}else{printf("no reflexive")};
 if (a = b && b = a){printf("symmetric")else{printf("no symetric")};
 if (a=b&&b=c&&c=a){printf("transitivity")else{printf("no transivity")};
 if (a*2=b*2){printf("the number are equaliy great!")else{printf("something was wrong")};
 return 0;
 
}
