#include <stdio.h>

#define MAX_LENGTH 50
#define ARRAY_SIZE 5

int main(){
char abc[ARRAY_SIZE][MAX_LENGTH]={"a","b","c","d","e"};
  //white a number to belong
  int x
  scanf("%d",&x);
  // compare with abc
  for (i=0;i<=sinzeof(abc);i++){
    if (x=abc[i]){
      printf("%d belong abc",x)
        }
  }
  // print the answer

return 0;
}
